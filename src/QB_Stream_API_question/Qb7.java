package QB_Stream_API_question;

import java.util.List;
import java.util.Arrays;
import java.util.stream.Collectors;

public class Qb7 {

	public static void main(String[] args) {
	
// From numbers(provided at top of doc),Print all numbers greater than 5
		
	List<Integer> numbers = Arrays.asList(1,2,3,4,5,6,8,10);
	
	//numbers.stream().filter(a -> a<5).forEach(b -> System.out.println(b));
//	   List<Integer> la=numbers.stream().filter(a -> a<5).collect(Collectors.toList());
//		System.out.println(la);
	}	
}
